import Chat from '@/components/Chat';
import { ChatInterface } from '@/components/ChatInterface';

/**
 * Home page component that renders the AI Study Buddy chat interface
 * Optimized for accessibility and students with learning disabilities
 */
export default function Home() {
  // return <ChatInterface />;
  return <Chat />;
}
